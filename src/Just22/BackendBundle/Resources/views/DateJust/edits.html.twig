<form action="{{ path('datejust_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="box column-right">
        <div class="box-header"><span class="glyph pencil"></span><h1>Editar</h1></div>
        <div class="box-content">
    {{ form_widget(edit_form) }}
            </div>
            <div class="action_bar">
                <button class="button blue" type="submit"><span class="glyph inbox"></span>Guardar</button>
                <a class="cierraventanita button red" onclick="cerrarventanita(); return false;">cerrar</a>
                    
                    <form action="{{ path('datejust_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
        <button class="eliminar_derecha button blue">
            <span class="glyph delete"></span>
            Eliminar
        </button>
</form>
            </div>
        </div>
    </form>



