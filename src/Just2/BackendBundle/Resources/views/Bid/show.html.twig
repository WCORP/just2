<div class="box column-right">
    <div class="box-header"><span class="glyph eye"></span><h1>Show</h1></div>
    <div class="box-content">
        <table class="record_properties">
            <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ entity.id }}</td>
                    </tr>
                    <tr>
                        <th>Price</th>
                        <td>{{ entity.price }}</td>
                    </tr>
                    <tr>
                        <th>Createdat</th>
                        <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="action_bar">
<a  class="button blue" href="{{ path('bid_edit', { 'id': entity.id }) }}">
    <span class="glyph pencil"></span>
    Editar
</a>
<form action="{{ path('bid_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
        <button class="eliminar_derecha button blue">
            <span class="glyph delete"></span>
            Eliminar
        </button>
</form>
                </div>
            </div>
        </div>


