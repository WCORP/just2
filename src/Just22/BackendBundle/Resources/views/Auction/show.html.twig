<div class="box column-right">
    <div class="box-header"><span class="glyph eye"></span><h1>Show</h1></div>
    <div class="box-content">
        <table class="record_properties">
            <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ entity.id }}</td>
                    </tr>
                    <tr>
                        <th>Reservation</th>
                        <td>{{ entity.reservation }}</td>
                    </tr>
                    <tr>
                        <th>Winningbid</th>
                        <td>{{ entity.winningBid }}</td>
                    </tr>
                    <tr>
                        <th>State</th>
                        <td>{{ entity.state }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="action_bar">
<a  class="button blue" href="{{ path('auction_edit', { 'id': entity.id }) }}">
    <span class="glyph pencil"></span>
    Editar
</a>
<form action="{{ path('auction_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
        <button class="eliminar_derecha button blue">
            <span class="glyph delete"></span>
            Eliminar
        </button>
</form>
                </div>
            </div>
        </div>


